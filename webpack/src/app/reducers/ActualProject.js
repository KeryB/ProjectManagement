
const initialState = {
    isLoading: false,
    isFetched: false,
    projectData:{}
};

export default (state = initialState, action = {}) => {


    switch (action.type) {

        default:
            return state;
    }
}